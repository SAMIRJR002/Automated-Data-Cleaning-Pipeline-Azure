# Azure-data-factory
Sales data cleaning pipeline built with Python (Pandas) and Azure Data Factory — handles nulls, mixed date formats, negative values, type casting, and category standardization on a real-world dataset.
A structured data cleaning pipeline that transforms 221 rows of messy real-world sales data into a reliable 112-row clean dataset — built with Python and documented with Azure Data Factory Data Flow expressions.
